<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="CarparkTracker.CarparkDetailPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Name"/>
            <Label Text="{Binding Name}"/>
            
            <Label Text="Name"/>
            <Label Text="{Binding Name}"/>

            <maps:Map WidthRequest="320" HeightRequest="200"
                    x:Name="MyMap" TranslationX="{Binding Coordinate.Longitude}" TranslationY="{Binding Coordinate.Latitude}"
                    IsShowingUser="False"  
                    MapType="Street">
            </maps:Map>


        </StackLayout>
    </ContentPage.Content>
</ContentPage>

<!-- the parking’s name, address, contact details and that shows a map with the parking’s location. 
    
    xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
                <maps:Map WidthRequest="300" HeightRequest="200" IsShowingUser="true" MapType="Hybrid"></maps:Map>
    -->